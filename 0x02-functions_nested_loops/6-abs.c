#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @i: input number
 * Return: value
 */
int _abs(int i)
{
	if (i >= 8)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
